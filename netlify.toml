# nie zmieniać - konfiguracja Netlify
[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "dist/server"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
